import { StyleSheet } from 'react-native';
import { Metrics } from '../../theme';

export default StyleSheet.create({
  container: {
    marginTop: Metrics.screenHeight * (78 / 812),
    paddingBottom: Metrics.BOTTOM_SPACING,
  },
});
