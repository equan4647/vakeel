import { StyleSheet } from 'react-native';
import { Colors } from '../../theme';

export default StyleSheet.create({
  circle: {
    backgroundColor: Colors.lightBlue41,
    borderWidth: 2,
    borderColor: Colors.lightBlue,
  },
});
