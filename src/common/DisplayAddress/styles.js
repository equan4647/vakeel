import { StyleSheet } from 'react-native';
import { Metrics } from '../../theme';

export default StyleSheet.create({
  headingStyle: { marginBottom: Metrics.ratio(15) },
  addressItem: { marginBottom: Metrics.ratio(15) },
});
