import { StyleSheet } from 'react-native';
import { Metrics, Colors } from '../../theme';

export default StyleSheet.create({
  seperator: { height: Metrics.ratio(30), backgroundColor: Colors.white },
});
