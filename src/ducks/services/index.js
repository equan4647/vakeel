import reducer from './reducer';
import * as servicesTypes from './types';
import * as servicesActions from './actions';
import * as servicesSelectors from './selectors';

export { servicesTypes, servicesActions, servicesSelectors };

export default reducer;
