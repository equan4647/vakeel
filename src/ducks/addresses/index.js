import reducer from './reducer';
import * as adressTypes from './types';
import * as adressActions from './actions';
import * as adressSelectors from './selectors';

export { adressTypes, adressActions, adressSelectors };

export default reducer;
