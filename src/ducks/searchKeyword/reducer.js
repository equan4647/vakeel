import { SEARCH_KEYWORD, RESET_SEARCH_KEYWORD } from './types';

const initialState = {};

/*
export default (state = initialState, action) => {
  const { identifier, data } = action;

  switch (action.type) {
    case SEARCH_KEYWORD.SUCCESS:
      return { ...state, [identifier]: data };
    case RESET_SEARCH_KEYWORD:
      return { ...state, [identifier]: [] };

    default:
      return state;
  }
};
*/
