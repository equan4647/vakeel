import reducer from './reducer';
import * as userRoleTypes from './types';
import * as userRoleActions from './actions';
import * as userRoleSelectors from './selectors';

export { userRoleTypes, userRoleActions, userRoleSelectors };

export default reducer;
