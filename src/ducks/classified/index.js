import reducer from './reducer';
import * as classifiedTypes from './types';
import * as classifiedActions from './actions';
import * as classifiedSelectors from './selectors';

export { classifiedTypes, classifiedActions, classifiedSelectors };

export default reducer;
